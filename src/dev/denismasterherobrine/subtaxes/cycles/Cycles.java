package dev.denismasterherobrine.subtaxes.cycles;

public class Cycles {
    long unixTime = System.currentTimeMillis() / 1000L; // Unix Time of current time
    long unixTimeMinutes = (System.currentTimeMillis() / 1000L) / 60L;
    long unixTimeHours = ((System.currentTimeMillis() / 1000L) / 60L) / 24L;
}
